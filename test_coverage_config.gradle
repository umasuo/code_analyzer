// define the code coverage requirment

jacocoCoverage { // Enforce minimum code coverage of 80% for every Java file.
  // Minimum 'branch' coverage of 80% for all files.
  fileThreshold 0.8, BRANCH
  fileThreshold 0.9, METHOD
}

